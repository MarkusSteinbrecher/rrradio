# Xcode build / derived state
build/
DerivedData/
.build/
*.xcuserdata
*.xcuserstate
xcuserdata/

# Generated by xcodegen from project.yml. The .xcodeproj + Info.plist
# are derived artifacts — re-run `xcodegen` to materialize them. If you
# prefer the Xcode-managed flow, swap project.yml for the .xcodeproj as
# the source of truth and remove these ignore lines.
*.xcodeproj/
rrradio/Resources/Info.plist

# Swift Package Manager
.swiftpm/
Package.resolved

# AppCode / IntelliJ
.idea/
